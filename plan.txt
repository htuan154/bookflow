GIAI ĐOẠN 1: NÂNG CẤP TRÍ TUỆ HỘI THOẠI & QUẢN LÝ DỮ LIỆU (HOÀN TẤT)
1.1. Xử lý hội thoại đa lượt (Multi-turn Context Memory) ✅ DONE

Trạng thái: Đã hoàn thành xử lý deep history scan và context trigger.


Kết quả: Bot đã nhớ được ngữ cảnh địa phương (Local Context) và xử lý các đại từ như "nó", "ở trỏng".

1.2. Tích hợp Tìm kiếm Vector (Semantic Search / RAG) ✅ DONE

Trạng thái: Đã hoàn thành tìm kiếm theo ý nghĩa và Auto-Tagging.


Kết quả: Hệ thống chạy ổn định với chiến lược tìm kiếm 2 bước (Strict/Global) và Hybrid Fallback.

1.3. Auto-Sync API (Pipeline dữ liệu tự động) ✅ DONE

Trạng thái: Đã chuyển đổi từ script thủ công sang API tự động.

Tính năng đã có:

API Endpoint đồng bộ tổng hợp (/sync) và thêm mới từng địa điểm/món ăn (/place, /dish).

Service tự động gọi Ollama sinh từ khóa và vector embedding ngay khi có data mới.


Hiệu quả: Admin thêm quán mới là Bot trả lời được ngay (Real-time Knowledge), không cần thao tác terminal.

1.4. Cơ chế tự sửa lỗi chính tả (Typo Correction) 📋 BACKLOG

Trạng thái: Tiếp tục giữ ở backlog vì Vector Search hiện tại đang xử lý lỗi nhập liệu khá tốt, chưa cần ưu tiên gấp.

GIAI ĐOẠN 2: MỞ RỘNG TÍNH NĂNG "TRAVEL AGENT" (LẬP KẾ HOẠCH & TIỆN ÍCH)
2.1. Tích hợp Dữ liệu thời gian thực (Real-time API) ✅ DONE

Trạng thái: Đã hoàn thành kết nối Weather và Map Service.


Kết quả:

Logic "Pure AI" không hard-code từ khóa, tự động suy luận điểm đến.

Bot trả lời được thời tiết (OpenWeatherMap) và khoảng cách (Google Maps) dựa trên ngữ cảnh cũ.

2.2. Tính năng Lập lịch trình tự động (Itinerary Generator) 🚀 NEXT STEP
Mục tiêu: Bot có thể gợi ý lịch trình đi chơi cụ thể (Sáng đi đâu, chiều ăn gì) thay vì chỉ gợi ý địa điểm lẻ tẻ.

Công việc cần làm:


Phân loại dữ liệu: Gắn nhãn thời gian phù hợp cho địa điểm (Ví dụ: Chợ đêm -> Tối, Bảo tàng -> Sáng/Chiều).


Thuật toán ghép điểm: Xây dựng logic ghép các điểm tham quan gần nhau về vị trí địa lý vào cùng một buổi.

Prompt Engineering: Tối ưu prompt để LLM sinh ra output dạng lịch trình (Timeline) dễ đọc.

2.3. Module Booking & Giá vé (Pending)

Kế hoạch: Mở rộng DB để thêm trường giá vé, giờ mở cửa và thu thập thông tin đặt phòng (Slot Filling).

GIAI ĐOẠN 3: TỐI ƯU VẬN HÀNH & CÁ NHÂN HÓA (ECOSYSTEM)
3.1. Xây dựng CMS Admin (Quản trị dữ liệu)

Kế hoạch: Xây dựng giao diện Web để quản lý địa điểm thay vì dùng Postman, tích hợp nút "Sync All Vectors".

3.2. Cá nhân hóa người dùng (User Personalization)

Kế hoạch: Lưu trữ và nhớ sở thích du lịch của người dùng (sang chảnh, bụi bặm, v.v.).

3.3. Analytics

Kế hoạch: Thống kê câu hỏi phổ biến và các trường hợp Bot phải dùng Fallback để cải thiện dữ liệu.